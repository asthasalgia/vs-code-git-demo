this is file 2
we will again version control it
pipeline {
    agent any tools {Maven 'Maven'}
}
stages { stage (Initialize)
steps { 
    sh'''
         echo "PATH = $ {PATH}"
         echo "M2_Home = $ {M2_HOME}"

}
}done
add: name
Run: Maven 